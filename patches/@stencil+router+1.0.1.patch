diff --git a/node_modules/@stencil/router/dist/collection/components/router/router.js b/node_modules/@stencil/router/dist/collection/components/router/router.js
index 4281dad..bab28dd 100644
--- a/node_modules/@stencil/router/dist/collection/components/router/router.js
+++ b/node_modules/@stencil/router/dist/collection/components/router/router.js
@@ -22,6 +22,19 @@ export class Router {
     constructor() {
         this.root = '/';
         this.historyType = 'browser';
+        // PATCH (polls.pizza): Stencil 3+ removed context props
+        // (`@Prop({ context: "queue" })`), so the compiler never injects
+        // `this.queue`, and `scrollTo()` crashes on `this.queue.write(...)`.
+        // Shim a minimal queue. See patches/stencil-router+1.0.1.patch
+        if (this.queue == null) {
+            var schedule = typeof requestAnimationFrame === 'function'
+                ? function (cb) { return requestAnimationFrame(cb); }
+                : function (cb) { return setTimeout(cb); };
+            this.queue = {
+                write: schedule,
+                read: schedule,
+            };
+        }
         // A suffix to append to the page title whenever
         // it's updated through RouteTitle
         this.titleSuffix = '';
diff --git a/node_modules/@stencil/router/dist/collection/components/switch/switch.js b/node_modules/@stencil/router/dist/collection/components/switch/switch.js
index 994505d..56c59cb 100644
--- a/node_modules/@stencil/router/dist/collection/components/switch/switch.js
+++ b/node_modules/@stencil/router/dist/collection/components/switch/switch.js
@@ -18,6 +18,20 @@ export class RouteSwitch {
     constructor() {
         this.group = getUniqueId();
         this.subscribers = [];
+        // PATCH (polls.pizza): Stencil 3+ removed context props
+        // (`@Prop({ context: "queue" })`), so the compiler never injects
+        // `this.queue`, and `componentUpdated()` crashes on
+        // `this.queue.write(...)`. Shim a minimal queue.
+        // See patches/stencil-router+1.0.1.patch
+        if (this.queue == null) {
+            var schedule = typeof requestAnimationFrame === 'function'
+                ? function (cb) { return requestAnimationFrame(cb); }
+                : function (cb) { return setTimeout(cb); };
+            this.queue = {
+                write: schedule,
+                read: schedule,
+            };
+        }
     }
     componentWillLoad() {
         if (this.location != null) {
