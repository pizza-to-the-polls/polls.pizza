diff --git a/node_modules/@stencil/router/dist/collection/components/router/router.js b/node_modules/@stencil/router/dist/collection/components/router/router.js
index 4281dad..cd45675 100644
--- a/node_modules/@stencil/router/dist/collection/components/router/router.js
+++ b/node_modules/@stencil/router/dist/collection/components/router/router.js
@@ -45,7 +45,10 @@ export class Router {
     }
     scrollTo(scrollToLocation) {
         const history = this.history;
-        if (scrollToLocation == null || this.isServer || !history) {
+        // PATCH (issue #118): `isServer` arrives via the context API, which
+        // Stencil 4 removed, so it can't be relied on. Also bail when the
+        // queue context is absent (SSR/hydration) — scrolling is browser-only.
+        if (scrollToLocation == null || this.isServer || !history || !this.queue) {
             return;
         }
         if (history.action === 'POP' && Array.isArray(history.location.scrollPosition)) {
diff --git a/node_modules/@stencil/router/dist/collection/components/switch/switch.js b/node_modules/@stencil/router/dist/collection/components/switch/switch.js
index 994505d..873d19d 100644
--- a/node_modules/@stencil/router/dist/collection/components/switch/switch.js
+++ b/node_modules/@stencil/router/dist/collection/components/switch/switch.js
@@ -61,7 +61,15 @@ export class RouteSwitch {
         activeChild.el.match = activeChild.match;
         activeChild.el.componentUpdated = (routeViewUpdatedOptions) => {
             // After the new active route has completed then update visibility of routes
-            this.queue.write(() => {
+            // PATCH (issue #118): Stencil 4 removed the context API, so `queue`
+            // is undefined everywhere (browser included). Fall back to running
+            // the update synchronously — skipping it entirely would leave the
+            // previous page visible and never notify the Router, breaking
+            // client-side navigation.
+            const schedule = this.queue
+                ? (fn) => this.queue.write(fn)
+                : (fn) => fn();
+            schedule(() => {
                 this.subscribers.forEach((child, index) => {
                     child.el.componentUpdated = undefined;
                     if (index === this.activeIndex) {
